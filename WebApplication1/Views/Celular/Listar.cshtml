@model IEnumerable<WebApplication1.Models.Celular>

@{
    ViewBag.Title = "Listar";
}

<h2>Lista de Celulares</h2>

<p>
    @Html.ActionLink("➕Cadastrar", "Create", null, new { @class = "btn btn-success" })
</p>
<table class="table table-hover table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Marca)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Novo)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Numero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Marca)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Novo)
            </td>
            <td>
                @Html.ActionLink("🖊Alterar", "Edit", new { id = Model.ToList<WebApplication1.Models.Celular>().IndexOf(item) }, new { @class = "btn btn-primary" })
                @Html.ActionLink("👁Visualizar", "Exibir", new { id = Model.ToList<WebApplication1.Models.Celular>().IndexOf(item) }, new { @class = "btn btn-secondary" })
                @Html.ActionLink("🗑Excluir", "Delete", new { id = Model.ToList<WebApplication1.Models.Celular>().IndexOf(item) }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>
