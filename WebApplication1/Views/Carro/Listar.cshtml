@model IEnumerable<WebApplication1.Models.Carro>

@{
    ViewBag.Title = "Listar";
}

<h2>Lista de Carros</h2>

<p>
    @Html.ActionLink("➕Cadastrar", "Create", null, new { @class = "btn btn-success" })
</p>
<table class="table table-hover table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ano)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Placa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cor)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ano)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Placa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cor)
            </td>
            <td>
                @Html.ActionLink("🖊Alterar", "Edit", new { id = Model.ToList<WebApplication1.Models.Carro>().IndexOf(item) }, new { @class = "btn btn-primary" })
                @Html.ActionLink("👁Visualizar", "Exibir", new { id = Model.ToList<WebApplication1.Models.Carro>().IndexOf(item) }, new { @class = "btn btn-secondary" })
                @Html.ActionLink("🗑Excluir", "Delete", new { id = Model.ToList<WebApplication1.Models.Carro>().IndexOf(item) }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>
